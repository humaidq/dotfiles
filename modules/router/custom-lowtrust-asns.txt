# Whole hosting networks dropped LAN->WAN for devices in the low-trust pool
# ONLY. Nothing here affects any other device on this LAN.
#
# One AS number per line, `#` comments. Expanded to CIDRs at build time from
# ip2asn-combined.tsv, so the ranges refresh whenever that table is updated
# rather than rotting the way a pasted list of prefixes would. An AS number that
# the table does not know fails the build rather than silently contributing
# nothing.
#
# WHY THIS EXISTS. Chasing individual nodes was losing: the tunnel client holds
# a pool of candidates, probes them, selects one, and refreshes the pool from a
# control channel fronted behind a CDN that cannot be blocked. Over a thousand
# addresses have been collected one at a time and the supply has not thinned.
# A pool device has no business reaching a rented VPS at all, so the whole
# market is a better unit than the individual host.
#
# WHY IT IS SAFE ENOUGH TO BE WORTH IT: the tunnel rents cheap VPSes, and
# mainstream app backends do not live there. AWS, GCP and Azure are absent from
# this list entirely, as are every CDN, so a pool device keeps WhatsApp, Botim,
# Apple, Google, Meta and ordinary browsing. What it loses is the small tail of
# services self-hosted on budget providers — real, occasional, and the accepted
# price. The generator refuses to build if any range here would swallow one of
# the addresses this network depends on; see the guard list in ip-blocklist.nix.
#
# Counted against the throttle list when chosen: these fifteen cover 88% of
# every node collected to date. Recompute rather than trusting that number —
# see the sweep in the throttle file's own notes for why counts here rot.

# --- The six general clouds, 71% of collected nodes on their own -------------
#
# Ordinary hosting companies with ordinary customers, which is exactly why this
# list is pool-only and would be indefensible network-wide.
14061   # DigitalOcean — 537 collected nodes, by far the largest single source
63949   # Akamai Connected Cloud (Linode) — 277
12876   # Scaleway / Online SAS — 137
16276   # OVH — 112
8560    # IONOS / Fasthosts — 92
24940   # Hetzner — 28

# --- Hosting whose business is substantially VPN exits -----------------------
#
# Lower collateral than the six above: these sell to the market this is aimed
# at. M247 in particular is a large share of the commercial VPN estate.
51430   # AltusHost — 73 collected nodes across five of its /24s
9009    # M247 — 63
63199   # CDS Global Cloud — 63
214623  # 2644819 Ontario Inc — 46, across three ranges
401418  # FARDA-IX — 14
16125   # Cherry Servers — 12
3223    # Voxility — 8
401776  # AS-CUSTOMER, a bare ARIN object with no netname behind it — 6
59645   # WYBT-NET — 2

# --- Added after auditing what the first fifteen missed ----------------------
#
# 13213 was an omission rather than a judgement: it fell between "general cloud"
# and "VPN hosting" when the tiers were drawn and landed in neither, despite
# being the fourth-largest source in the throttle list. It spans 82.163,
# 107.182, 173.244, 216.119 and 88.202, and its 107.182.236.0/24 is midphase
# shared hosting — which is why individual /32s there were never a range case,
# and why the AS is the right unit instead.
13213   # THG Hosting / UK2NET — 53 collected nodes
#
# AS402267 Hadoopsky LLC belongs here on the merits — a small US hoster with
# three collected nodes — but it CANNOT go here: the shipped ip2asn table has
# never heard of it, because its allocation is dated 2025-04-07 and the table
# predates that. Adding the number fails the build with "no ranges found", which
# is the generator working correctly rather than a problem to route around. Its
# two /24s are listed by hand in custom-lowtrust-subnets.txt instead. Move it up
# here if that table is ever refreshed.

# Two Canadian ISPs rather than VPS shops, so this is a wider net than the rest
# of the file: it covers their whole customer base, not a rented-instance pool.
# Taken because 38 collected nodes sit in them and nothing on this network has
# any other reason to reach either. If a pool device ever needs something in
# Canada, these two are the first entries to question.
21949   # Beanfield Technologies — 25
63127   # MAPL-NET — 13

# --- Cogent, added on request after being argued against -------------------
#
# AS174 is TIER-1 TRANSIT, not a hosting company, and it is by a distance the
# widest entry in this file: 8,991 prefixes and 24 million addresses — more than
# every other AS here put together, and 0.56% of the routed IPv4 internet.
#
# The objection, recorded because it was raised and overruled rather than
# missed: a transit AS announces space on behalf of its customers, so this drops
# traffic to networks that have nothing to do with the twenty-five nodes that
# put it here. There is no way to tell from the announcement which of those
# 24 million addresses is a tunnel node and which is somebody's mail server.
#
# The counter-argument, which is what carries it: this is pool-only. The
# devices it applies to have already been judged not to need the open internet,
# and no other device on this LAN is affected by a single rule in this file.
#
# If a pool device starts failing to reach something ordinary and no other entry
# explains it, THIS IS THE FIRST ENTRY TO REMOVE. It is also the one most likely
# to be the cause, simply by size.
174     # Cogent — 25 collected nodes, 24M addresses of collateral
#
# STILL NOT COVERED, and not coverable this way — recorded so the gap is known
# rather than rediscovered:
#
#   ~25 singletons on residential and mobile carriers — Myanmar, Iran, Mexico,
#   Vietnam, Switzerland. These are the residential-exit pool, and an ASN list
#   of hosting providers can never catch them; that is what the per-address
#   rows in custom-throttle-list.txt are for.

# --- Zenlayer, added 2026-08-14, reversing the decline recorded below -------
#
# This entry was refused above the line for a while on two grounds. One of them
# turned out to be wrong and the other is still not satisfied, so the reversal
# is written out in full rather than just deleting the old note.
#
# WHAT THE DECLINE SAID: "an edge/CDN provider rather than a VPS shop, and 929
# prefixes of edge capacity is the kind of range that breaks something a phone
# actually needs. Ten nodes is not worth that. Revisit only with evidence of it
# carrying a tunnel."
#
# THE SIZE CLAIM WAS WRONG, and by enough to change the answer. Counted from
# the shipped ip2asn table: AS21859 announces 571 IPv4 ranges totalling 656,640
# addresses, which collapse to 775 CIDRs — 0.015% of routed IPv4. AS174 Cogent,
# already in this file and argued through at length above, is 24,004,735
# addresses. Zenlayer is THIRTY-SIX TIMES SMALLER than the widest entry already
# accepted here. "929 prefixes of edge capacity" was the number that carried
# the decline and it does not survive being computed.
#
# THE CARRYING EVIDENCE IS STILL ABSENT, and this is the honest gap. Across
# every capture taken 2026-08-12 to 14 there is not one established session to
# a Zenlayer address: no TLS, no SNI, no payload, 756 bytes inbound in total.
# The literal condition the decline set has NOT been met.
#
# WHAT REPLACED IT is a different and better argument, from 192.168.50.250's
# capture of 2026-08-13. The client ICMP-pings a spread of Zenlayer addresses
# and they REPLY — 98.98.207.98, .99, .102, .103, .105, 128.14.65.31, .34, .97,
# .150, 128.14.66.38, all alive, TTL 45-56. It then selects 98.98.207.106 and
# fires 1341 SYNs at tcp/443, which get nothing back. That is the same
# ping-then-probe-then-select cycle this file was written about, and it is
# running over CONTIGUOUS space: seven addresses inside 98.98.207.97-.106 and
# four inside 128.14.65-66. A client working consecutive addresses in one
# provider's block is the exact case where per-/32 rows lose and the AS is the
# right unit. Thirteen Zenlayer /32s are in the throttle list and nine more
# appear in that capture alone without ever having been added.
#
# So: not "it carried a tunnel", but "it is being enumerated as a pool, and the
# collateral is a fortieth of what the decline assumed". Pool-only, like
# everything here. The never-cover tripwire was dry-run against these 775 CIDRs
# before the entry went in and matches nothing.
#
# WHAT THE COLLATERAL ACTUALLY IS, added 2026-08-22 and missing when this entry
# went in. ZENLAYER IS A BYTEDANCE NETWORK PROVIDER. TikTok's real-time comms
# and TikTok LIVE ride Zenlayer addresses, on opaque high ports, with no DNS
# naming the peer and with relay-failover-to-a-fresh-address when one goes away
# — which is to say they are INDISTINGUISHABLE ON FLOW SHAPE ALONE from what
# put this entry in the file.
#
# That is not hypothetical here. On 2026-08-05 a run of Zenlayer addresses on
# ports 29009/30303/50000 was read as imo on exactly that reasoning — cloud
# host, arbitrary high port, opaque payload, failover — and six were blocked,
# cutting a live broadcast repeatedly. It was TikTok LIVE. Two checks would have
# caught it and neither needs a capture: DIRECTION (that flow was 5.5:1
# upload-heavy, i.e. broadcasting; an imo video call is roughly symmetric) and
# THE DEVICE'S OWN DNS IN THE SAME WINDOW (rtcpc-access-fr.tiktokv.com,
# byteglb.com, widget-sg.tiktokv.com, and no imo names at all).
#
# SO THE SYMPTOM TO EXPECT FROM THIS ENTRY IS NAMED, rather than left as "a
# pool device loses something": TikTok LIVE and TikTok's RTC breaking for pool
# devices while ordinary TikTok keeps working. That is a REAL cost and not
# obviously an intended one — this repo blocks TikTok LIVE deliberately at the
# IP level in custom-ip-blocklist.txt and by name in custom-blocklist.txt, and
# an ASN entry admitted on tunnel-enumeration grounds should not be the thing
# quietly widening that policy. Anyone reading a Zenlayer flow as a tunnel must
# check direction and the device's DNS first, every time.
#
# NOT AFFECTED, checked 2026-08-22, so this is not read as a live breakage: the
# Zenlayer addresses in custom-ip-blocklist.txt (23.90.158.131/.238,
# 98.98.250.236, 98.98.254.102/.107) sit in the TikTok Live section and are
# dropped on purpose under that policy, not left over from the 2026-08-05
# misattribution. Ports 29009/30303/50000 are not in custom-port-blocklist.txt.
#
# IF A POOL DEVICE LOSES SOMETHING, Cogent above is still the first entry to
# try on size alone. This is the second, and the gap between them has narrowed:
# it remains the only entry in the file admitted without direct evidence of
# carrying traffic, and it is now also the only one with a named, plausible
# collateral victim rather than a hypothetical one. If what broke is TikTok
# LIVE or a call, try this one FIRST and Cogent second.
21859   # Zenlayer — 13 collected nodes, 656k addresses, enumerated as a pool

# --- The reseller estate, added 2026-08-14 -----------------------------------
#
# Eight ASNs, 73,728 addresses between them — 0.0017% of routed IPv4, against
# the 39,993,747 the twenty entries above already cover. This section is the
# cheapest thing in the file by three orders of magnitude and it is aimed at
# the best-evidenced target in it.
#
# WHAT TIES THEM TOGETHER is a registration template, written up at length in
# custom-throttle-list.txt's 2026-08-14 blocks. Every address collected from
# these ASNs sits in a /24 sub-allocation whose netname is mechanically derived
# from the prefix (NET-169-40-141-0-24, NET-95-134-21-0-24, NET-167-148-28-0-24,
# NET-46-183-24-0-24, NET-141-11-105-0-24 …), whose org-name is the anonymous
# "Private Customer", which has no PTR, and whose registered country disagrees
# with the country the prefix is announced from. One reseller is selling /24s
# across eight small hosting ASes and the client shops across all of them.
#
# THE CARRYING EVIDENCE IS DIRECT, which is what the Zenlayer note above had to
# do without. From 192.168.50.162's captures of 2026-08-14, 15:15 and 15:29:
#
#   AS393942  45.151.143.134   494 kB, TLS on 443 with SNI yahoo4.com
#   AS135402  95.134.64.103    594 kB, TLS on 443 with SNI rebel2.com
#   AS137517  169.40.141.61     46 kB, TLS on 443 with SNI rochester2.com
#   AS22427   167.148.28.60     57 kB, opaque TCP on port 27160, no TLS
#   AS31715   141.11.105.190/.214, dialled and RST-refused
#   AS142146  46.183.24.168-.171, in custom-imo-list.txt, .171 at 540 kB
#
# THE THREE SNIs ARE THE FINDING. yahoo4.com, rebel2.com and rochester2.com are
# one naming scheme — an ordinary English word or brand, a single digit, .com —
# and not one of them is the certificate the host actually holds.
# rochester2.com has NEVER BEEN REGISTERED (registry says "No match"), so no
# client can ever have resolved it; rebel2.com was registered in 2017 through a
# domain reseller and yahoo4.com in 2025 through Alibaba, and neither has any
# relationship to the address dialling them. These are cover names attached to
# hardcoded addresses so the handshake looks ordinary to anything reading SNI.
# The name layer cannot reach any of it, because nothing is ever resolved.
#
# COLLATERAL. All eight are small hosting ASes with 11 to 45 announced ranges
# each. None is a CDN, none is transit, and none appears anywhere in this
# network's ordinary traffic across either capture except as the addresses
# above. Three of them — AS137517, AS135402 and AS22427 — are already named in
# custom-ip-blocklist.txt as the rented half of the imo/BIGO estate, so their
# other use here is an app estate this network already drops. The never-cover
# tripwire was left at full strength and the build passes, so none of these
# ranges touches the resolver, the Akamai edges, STUN or this operator's hosts.
#
# The four WiredISP shells go in together. AS393942 is the one with carrying
# evidence; AS21840, AS135402 and AS137235 are the same operator under
# different numbers, named as a set in custom-ip-blocklist.txt, and splitting
# them would leave the obvious next hop open.
393942  # WiredISP Inc. — 9,216 addresses, SNI yahoo4.com on 45.151.143.134
21840   # NET3, WiredISP shell — 11,008
135402  # US-FXCAPITAL, WiredISP shell — 11,776, SNI rebel2.com on 95.134.64.103
137235  # KMS-AS-AP, WiredISP shell — 8,448
142146  # Sandhill Solution — 2,816, the smallest entry in this file
22427   # GNET — 9,984, opaque tcp/27160 on 167.148.28.60
137517  # FlameHosting — 10,240, SNI rochester2.com on 169.40.141.61
31715   # CloudIP — 10,240

# --- NovoServe, added 2026-08-22 ---------------------------------------------
#
# Three ASNs, 21,760 IPv4 addresses between them, which puts this section in
# the same cheap tier as the reseller estate above rather than anywhere near
# the transit entries. A Dutch bare-metal host: dedicated servers and
# sub-allocated /24s, no CDN, no transit, no eyeball network.
#
# THE STANDING OBSERVATION IS THE REASON IT IS HERE. NovoServe keeps recurring
# as a proxy endpoint. Said plainly because a later reader cannot otherwise tell
# how much of this is measured: ONE address is directly evidenced, and before
# this section not a single NovoServe address appeared in the throttle list, the
# IP blocklist or the imo list.
#
# THE EVIDENCED ADDRESS is 102.129.246.182, in AS215224's 102.129.246.0/24,
# reached on tcp/9153 as a sole peer — a V2Ray/Xray-family proxy endpoint.
#
# IT IDENTIFIED ITSELF AT THE RESOLVER, which is the reusable part. A client of
# that family configured with a bare `IP:port` server address hands the whole
# string to its stub, which cannot parse it and asks for it as a name — so the
# resolver logs `102.129.246.182:9153` as an A/AAAA/HTTPS question, usually next
# to a `_dns.resolver.arpa` SVCB for encrypted-DNS discovery. A question_name
# containing a colon is never a hostname, and it yields the endpoint and the
# port with no packet capture involved.
#
# THE CERTIFICATE TEST DOES NOT DECIDE THIS ONE, which is worth recording
# because this file's neighbours lean on it. The endpoint serves a GENUINE
# Let's Encrypt R13 certificate — random serial, 90-day validity,
# CN=fleetwoord.akherrosh.fun — not the self-signed or absurd-validity
# camouflage described in custom-throttle-list.txt. VLESS and Trojan endpoints
# use real ACME certs precisely so that check comes back clean. What decides it
# instead is a random-word .fun subject on a rented single-tenant box reached at
# a bare IP:port, by a client that resolves nothing else.
#
# AS215224 IS THE ONE THAT MATTERS and it matches the reseller template written
# up above: it is literally "NovoServe Customers ASN", 32 sub-allocated /24s,
# and the observed prefix is registered through AFRINIC as ZA while being
# announced by a Netherlands operator — the same registered-country-disagrees
# tell as the eight ASNs in the previous section. Its neighbours are the usual
# proxy-reseller space (154.6.x, 45.142.x, 176.111.47.0/24, 185.224.217.0/24).
#
# AS24875 and AS209445 go in with it as the same operator under different
# numbers, the same reasoning as the four WiredISP shells above. Neither has
# carrying evidence of its own and both are small enough that splitting the
# operator would cost more than it saves.
#
# COLLATERAL. Nothing on this network has ever reached NovoServe space for any
# other reason — zero addresses across all three lists before today. The
# never-cover tripwire was left at full strength and the build passes, so none
# of these ranges touches the resolver, the Akamai edges, STUN or this
# operator's own hosts.
#
# NOT PUT IN custom-cdn-quota-asns.txt, which was the other candidate: that file
# shapes instead of dropping because its targets are shared CDN edges the whole
# house depends on. NovoServe is single-tenant hosting with no such edge, so the
# reason to prefer shaping over dropping does not apply and the ordinary
# pool-only drop is the right instrument. 102.129.246.182 is ALSO in
# custom-throttle-list.txt as a /32 — that is deliberate and not a reversed
# decision; see the note there, which turns on this pool being keyed on MAC
# while the clients concerned randomise theirs.
24875   # NovoServe — 11,520, the operator's own network
215224  # NovoServe Customers ASN — 9,984, holds 102.129.246.0/24
209445  # NovoServe Denmark — 256, one /24

# --- AS8849 Melbikomas, 2026-08-28: THE FIRST ENTRY HERE ADDED ON ONE ---------
# --- OBSERVATION RATHER THAN AN ACCUMULATED POOL -----------------------------
#
# Every other line in this file cites a count of collected nodes, because each
# was a consolidation: enough /32s had piled up in custom-throttle-list.txt that
# the AS became the cheaper unit. THIS ONE HAS A COUNT OF ZERO. Nothing in the
# throttle list, the IP blocklist or the imo list falls anywhere in AS8849. It
# is a provider this network had never recorded before today.
#
# WHAT IT RESTS ON INSTEAD. A 30-second full-LAN capture at 14:00 on 2026-08-28:
# 192.168.50.198 (aa:23:76:db:0c:3a, a pool device) sent 1,928 of its 1,948
# frames to 146.19.196.200:59001 — 2.03 MB down and 62 kB up in 26 seconds,
# 1,506 of those packets exactly 1,288 bytes, UDP and TCP to the same IP:port
# at once, opaque from byte 0, one DNS query in the window (www.google.com, a
# connectivity check) and no SNI anywhere. Resolution had moved inside it.
#
# THE PART THAT JUSTIFIES AN AS RATHER THAN A /32. The TCP side frames as
# `0200 eff0` — a two-byte length then a two-byte magic — which is the identical
# structure to the `0200 6263` recorded for 192.168.50.221 against a different
# provider entirely. One client on two handsets, differing only in the magic,
# already seen moving Leaseweb -> FranTech within a day of a /32 being written
# for it. A /32 here would be outrun the same way; the AS at least covers the
# hop within this provider.
#
# COLLATERAL: 13,824 addresses across 33 ranges, which puts it among the
# SMALLEST entries in this file — an order of magnitude under Zenlayer, three
# under Cogent. Melbikomas is pure budget hosting, the same category as M247,
# AltusHost and NovoServe above, so the header's argument that mainstream app
# backends do not live here applies unchanged. The build guard was checked
# explicitly: none of the eight never-cover addresses falls inside any AS8849
# range, so this cannot swallow the resolver, the STUN host or hisn.
#
# BE READY TO REVISIT IT. On this file's own standards a single observation is
# thin, and it is recorded that way deliberately rather than dressed up with the
# node count the other lines carry. If nothing further is ever seen in AS8849,
# this is a candidate for removal rather than a precedent to cite.
8849    # Melbikomas UAB — 13,824, one observed tunnel endpoint, no collected pool
#
# INCOMPLETE ON ITS OWN — SEE AS56630 BELOW. Melbikomas announces from two AS
# numbers and this is the smaller one. Added first only because it was the one
# the .198 capture happened to land in; it covers about a fifth of the estate.

# --- AS56630 Melbikomas, the OTHER half, 2026-08-28 --------------------------
#
# THE SAME PROVIDER AS AS8849 ABOVE, AND FOUR TIMES LARGER: 55,808 addresses
# over 72 ranges against 13,824 over 33. Adding AS8849 this morning looked like
# it settled Melbikomas and did not — it covered roughly a fifth of what the
# operator announces.
#
# HOW THE GAP WAS FOUND, because it is the transferable part. Not from traffic:
# 213.183.53.76 is a node hardcoded in the com.rapidconn.android APK, and when
# it was checked against this file it came back uncovered despite Melbikomas
# already being here. An AS entry is only as good as the assumption that a
# provider uses one AS number, and that assumption is worth testing by looking
# the provider up by NAME in ip2asn-combined.tsv rather than by the number you
# already have. Worth repeating for the other hosting entries in this file.
#
# Collateral is the usual budget-hosting bet, and at 55,808 it is still an order
# of magnitude under Zenlayer and three under Cogent, both long-standing entries
# here. The build guard was checked: none of the eight never-cover addresses
# falls in any AS56630 range.
#
# 213.183.53.76 itself is a /32 in custom-ip-blocklist.txt rather than here,
# because it is a Rapid VPN control-plane/node address and that file drops it
# for the whole LAN. This entry is the pool-only net around it.
56630   # Melbikomas UAB, second ASN — 55,808, holds 213.183.53.76

# --- AS209181 and AS154317, 2026-08-28, from the RESOLVER not a capture -------
#
# Both came out of a fortnight of blocky's query log (14-28 Aug) rather than a
# pcap, via the `IP:port` leak: a proxy client handed a literal endpoint
# resolves it as a hostname, so the address and port land in the log together.
# Seven such endpoints appeared; all seven are now /32s in
# custom-throttle-list.txt, and these two ASes are where the uncovered ones sat.
#
# AS209181 — ZENEX5IVE, 11,520 addresses over 33 ranges, so mid-table here and
# a shade under NovoServe. It is the best-attributed entry in this file: probed
# three times, 191.96.158.50 returns an identical Let's Encrypt certificate
# reading CN=fanzone.symlexvpn.com. The service names itself. Two collected
# nodes, .50 and .51, adjacent in 191.96.158.0/24 — and .51 had been throttled
# for weeks while .50 sat untouched one address away, which is the argument for
# taking the AS rather than continuing to add neighbours one at a time.
209181  # ZENEX5IVE / Symlex VPN — 11,520, named on its own certificate

# AS154317 — VyomCloud, and at 512 addresses in a single /23 it is the SMALLEST
# entry in this file bar NovoServe Denmark. One collected node,
# 160.250.224.190:4439, silent on 443 across three probes in the way a
# proxy-only node is. At this size the ASN is barely wider than the /32 and the
# collateral question hardly arises; the reason to take it at all is that the
# whole /23 is one small tenant's, so a hop within it is the likely next move.
154317  # VyomCloud — 512, one /23, one collected node

# --- AS14315, 2026-08-28, added on request; provenance is THIS REPO ----------
#
# 1GSERVERS, US budget VPS hosting — the same market as everything in the first
# section, and the reason it qualifies at all.
#
# NO CAPTURE AND NO NETWATCH OBSERVATION BACK THIS. It was requested, not
# collected, and `peer_obs` holds nothing in its space. Recorded plainly so a
# later reader does not credit it to evidence it does not have — the Cogent
# entry above sets the precedent for filing a requested AS honestly.
#
# What it does have is three addresses already dropped one at a time. The
# undocumented `# custom` block in custom-ip-blocklist.txt holds four rows, and
# three are 1GSERVERS: 206.168.190.255, and 136.175.9.11 and 136.175.8.145 —
# the last two inside the SAME 136.175.8.0/22. That is the AS209181 argument
# repeating: neighbours taken singly while the rest of the range sits open. The
# fourth, 204.77.223.251, is AS53767 ICASTCENTER and unaffected by this entry.
#
# SIZE: 26,368 IPv4 over 59 CIDRs, plus six IPv6 ranges (2605:8340::/32 the
# widest). Larger than any reseller-estate entry above, far short of Zenlayer
# (656k) or Cogent (24M). Hosting rather than transit, so the objection filed
# against those two does not apply here.
#
# NEAR MISS ON THE GUARD, worth recording because the next table refresh could
# turn it into a real one: 1GSERVERS announces 45.59.104.0/24, and hisn — this
# network's own huma.id and nebula lighthouse — is 45.59.120.67. Different /24s
# in the same /16, so lowTrustNeverCover does not fire today. Verified against
# all 61 rows at time of writing, no hit.
#
# The three /32s STAY in custom-ip-blocklist.txt. This entry covers pool
# devices only; those rows cover every other device on the LAN, which it does
# not touch. Same division as the Hetzner note there.
14315   # 1GSERVERS — 26,368, requested; three /32s already taken piecemeal
#
# DELIBERATELY NOT HERE:
#
# AS4637 TELSTRA GLOBAL, requested on 2026-08-14 and declined on the capture.
# The addresses that prompted it are the 42.99.140.x cluster — .138, .139,
# .152, .170, .194, .200, .202, .211, eight of them, several carrying megabytes
# — and they are NOT a tunnel. Every one dials tcp/443 with a real TLS
# handshake and a real, resolvable SNI in tiktokcdn.com or tiktokv.com:
# sf16-ies-music-sg.tiktokcdn.com, p16-common-sign.tiktokcdn.com and so on.
# That is TikTok's CDN edge riding Telstra Global transit.
#
# It matters that this repo blocks TikTok LIVE and not TikTok. custom-blocklist
# .txt anchors on the webcast/pull-/push- prefixes precisely so ordinary TikTok
# keeps working, and none of the names in this capture matches those. So
# AS4637 would not break a tunnel, it would break TikTok — for pool devices
# only, but deliberately, and by an entry filed as if it were a VPN network.
# At 1,132,544 addresses it is also transit rather than hosting, the same
# objection recorded against Cogent above.
#
# If TikTok should be off for pool devices, that is a legitimate decision and a
# different one, and it belongs in custom-blocklist.txt where the live-only
# scoping is argued — not here, disguised as a tunnel finding.
#
# AS3257 GTT (4,952,320) and AS60068 CDN77 (119,552). 195.21.131.78 on GTT is a
# genuine tunnel node — opaque UDP on port 4452 — and it is in the throttle
# list as a /32. But GTT is tier-1 transit and CDN77 is a CDN serving
# tiktokcdn.com in this same capture. Both are the "refuse, add nothing" case.
#
# AS9304 Hutchison / HGC (1,874,175) announces 213.130.159.0/24, where ten
# collected addresses now sit. The /24 is in the throttle list instead; a Hong
# Kong carrier's whole network is not worth one reseller's block.
#
# CANDIDATES NOT TAKEN, recorded so they are not rediscovered as new:
# AS54994 Meteverse (253,696) is named in custom-ip-blocklist.txt with the
# imo/BIGO estate and 138.113.178.42 in it is already dropped, but nothing in
# either capture shows it carrying a tunnel. AS139325 IQ-TEL (1,536, and the
# smallest thing considered here) holds 103.141.64.165, which does carry one —
# but it is a Bangladeshi ISP's subscriber base, the residential-exit case this
# file's closing note says an ASN list can never properly cover. Both are one
# piece of evidence away.
#
# AWS, GCP, Azure. They carry the backends of nearly every app on a normal
# handset, and adding them would break the device rather than the tunnel.
#
# THE OLD REASON FOR THAT — "not one collected node sits in any of them" — IS
# NO LONGER TRUE and was corrected on 2026-08-14. Three AWS instances are now
# in the throttle list: 13.39.77.133 (eu-west-3), 13.49.121.85 (eu-north-1,
# 1,039 kB of opaque tcp/27649) and 16.171.185.251 (eu-north-1, 885 kB of
# opaque tcp/35585). The client has started renting in AWS. The conclusion is
# unchanged and now rests only on the collateral argument, which is the one
# that was always doing the work — but the factual claim underneath it has
# gone, so per-/32 rows in the throttle list are the whole of the answer for
# AWS and will have to keep up on their own.

# --- The VPN Master / Snap Master estate, ten ASNs, 2026-08-30 --------------
#
# ADDED BECAUSE THE /32s WERE VISIBLY LOSING. Three 10-minute captures show two
# pool devices probe 497 distinct candidate addresses and then select from them,
# refreshing the pool from a control channel on tcp/20028 across eight AWS and
# GCP regions. 502 of those candidates are now in custom-throttle-list.txt, and
# a fresh node outside all of them (162.128.87.183) was handed out the same
# evening. That is the pattern this file's own header describes, playing out
# again.
#
# AS62610 IS THE ONE THAT MATTERS AND THE REASON THE REST WERE MISSED. Both
# carrying sessions in those captures ran on it, and it was in no list at all —
# while 349 of the 497 candidates were ALREADY pool-covered by Linode, Scaleway,
# Zenlayer AS21859 and OVH above. The client did not pick AS62610 by chance; it
# went where the shaping was not. Zenlayer announces this estate from at least
# two ASNs and only one of them was listed, which is the specific gap to
# remember: checking AS21859 alone reads as covered when it is not.
#
# EVERY ONE OF THE TEN IS CONFIRMED BY CERTIFICATE, not inferred from hosting a
# node. A sample of 48 candidates spread across all of them runs the same three
# OpenSSH builds and serves the same no-subject, no-issuer,
# Jan 10 1923 -> Nov 22 2122 certificate, SHA-256 35:E9:C2:D2:...:F7:D6 — one
# image and one private key. Zero misses in the sample.
#
# COLLATERAL, stated honestly: these ten cover roughly 891,000 addresses, and
# they are budget and mid-tier hosting with real other tenants — Contabo and
# Clouvider especially sell to ordinary customers. A pool device loses anything
# self-hosted there. That is the same accepted price the header sets out for the
# six general clouds, and it stays defensible only because this file is
# pool-only.
#
# THREE DELIBERATELY NOT TAKEN, recorded so they are not rediscovered as new.
# All three hold confirmed nodes and are covered by /32s in the throttle list
# instead:
#   AS3257  GTT — a transit backbone. Its address space is other people's
#           traffic by definition.
#   AS9318  SK Broadband — a Korean residential ISP, the subscriber-base case
#           this file's closing note says an ASN list can never cover properly.
#   AS834   IPXO — an address-lease broker, so its ranges belong to whoever is
#           renting this month and say nothing about who is on them.
# Worth knowing that ip2asn-combined.tsv puts the estate's 82.40.48.0,
# 66.93.169.0 and 23.27.112.0 ranges in the first two of those, while a live
# lookup calls all three Zenlayer AS21859. Where the table is stale, neither
# ASN entry reaches the range and the /32 is the only thing that bites.
62610   # Zenlayer ZEN-DPS — 28 candidates observed, and both carrying sessions
61272   # UAB Bacloud — 15, PTRs all 7673-*.bacloud.info, one customer
401152  # ADCIL — 12
213650  # CDNINFRA — 8, and the smallest range here at 515 addresses
63023   # GTHost — 5
20326   # TeraSwitch — 5
62240   # Clouvider — 3
62563   # GlobalTelehost — 2
40021   # Contabo — 2
34989   # ServeTheWorld — 2
